{
  "name": "forge-studio-lite",
  "version": "1.0.0",
  "private": false,
  "license": "MIT",
  "type": "module",
  "description": "Generador de carruseles de Instagram — slides HTML consistentes con tu marca → PNGs 1080×1350 vía Playwright. Local, determinístico, $0.",
  "scripts": {
    "carousel": "tsx --env-file=.env src/pipelines/carousel.ts",
    "carrusel": "tsx --env-file=.env src/pipelines/carousel.ts",
    "scrape-images": "tsx --env-file=.env scripts/scrape-carousel-images.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "playwright": "^1.49.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
